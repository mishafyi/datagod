fastapi>=0.115
uvicorn>=0.34
httpx>=0.28
python-dotenv>=1.0
tenacity>=8.2
yfinance>=0.2.40
pandas>=2.0    # imported directly (yfin, scholar) — don't rely on it transitively via yfinance
requests>=2.31  # imported directly (scholar) — don't rely on it transitively via yfinance
selectolax>=0.3.21
feedparser>=6.0.12
beautifulsoup4>=4.12
lxml>=5.0  # yfinance get_earnings_dates() -> pandas.read_html needs it
