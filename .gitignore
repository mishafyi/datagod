# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# Secrets / local env
.env
.env.*
!.env.example

# Local agent / editor state
.claude/
.DS_Store

# Private local runbooks / machine-specific docs — kept on this machine,
# deliberately not part of the public repo
DEPLOY-NEW-PROJECT-COOLIFY.md
docs/AGENT_QUICKSTART.md
USE-DATAGOD-FROM-COOLIFY.md

# Large local artifacts — NOT part of the runtime image
# (Dockerfile only copies app/ + requirements.txt)
data/
research/
docs/obsidian-help/

# Test run outputs
tests/reports/
